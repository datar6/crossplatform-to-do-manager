{
  "name": "todo-electron",
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "compile": "tsc",
    "dev": "npm run compile && cross-env NODE_ENV=development electron .",
    "build:frontend": "cd ../../app && npm run build",
    "build": "npm run build:frontend && tsc && electron-builder",
    "pack": "electron-builder --dir"
  },
  "dependencies": {
    "electron": "^27.0.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "electron-builder": "^24.6.0",
    "typescript": "^5.3.0"
  }
}
